## PostgreSQL
spring:
  datasource:
    url: jdbc:postgresql://localhost:5436/fraud
    username: fraud
    password: fraud

  ## default connection pool
  hikari:
    connectionTimeout: 20000
    maximumPoolSize: 5

  mvc:
    hiddenmethod:
      filter:
        enabled: true
logging:
  level:
    org:
      springframework:
        jdbc:
          core:
            JdbcTemplate: debug

server:
  port: 8082
  servlet:
    session:
      cookie:
        http-only: false